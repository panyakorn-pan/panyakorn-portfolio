# คู่มือส่งต่องาน (สำหรับ AI ในแชทใหม่)

> ไฟล์นี้ไว้ให้ผู้ช่วย AI อ่านตอนเริ่มแชทใหม่ จะได้รู้ว่าโปรเจกต์นี้คืออะไร ทำถึงไหนแล้ว
> และมี "กติกา" อะไรที่ห้ามพลาด — **อ่านไฟล์นี้ให้จบก่อนแก้โค้ดใดๆ**
> ส่วนวิธีแก้เนื้อหาเว็บทีละจุด อยู่ใน `README.md` (เขียนไว้สำหรับเจ้าของเว็บ)

---

## 1. โปรเจกต์นี้คืออะไร

เว็บพอร์ตโฟลิโอส่วนตัวของ **ปัณยกร สิงห์ดวง (Panyakorn Singhadoung)** นักเรียนมัธยม
สายหุ่นยนต์ / AI / วิศวกรรม ใช้เพื่อโชว์ผลงานและใช้ประกอบการสมัครเรียน

- **เขียนด้วย HTML + CSS + JavaScript ล้วน** ไม่มี framework ไม่มี build step
- **ที่อยู่ในเครื่อง:** `C:\Users\lenovo\Desktop\panyakorn-portfolio`
- **GitHub:** https://github.com/panyakorn-pan/panyakorn-portfolio (branch `main`)
- **เว็บจริง:** https://panyakorn-pan.github.io/panyakorn-portfolio/
- **รันดูในเครื่อง:** มี `.claude/launch.json` ตั้ง python http.server พอร์ต 8080 ไว้แล้ว

**ธีม:** ขาวดำ (monochrome) มินิมอล เน้นตัวอักษรและเส้นบาง
ฟอนต์ Space Grotesk (หัวข้อ) + Noto Sans Thai (เนื้อหา) สีหลักอยู่ใน `:root` ของ `css/style.css`

---

## 2. โครงสร้างไฟล์

| ไฟล์ | หน้าที่ |
|---|---|
| `index.html` | หน้าแรก (ชื่อ + 2 บล็อกพรีวิว) |
| `portfolio.html` | Hall of Frame — ตารางการ์ดผลงาน + ปุ่มกรอง |
| `project.html` | หน้ารายละเอียดผลงาน (ไฟล์เดียว ใช้ร่วมทุกงาน อ่าน `?slug=`) |
| `about.html` | My Profile — แนะนำตัว + ไทม์ไลน์การศึกษา + เรื่องเล่าวัยเด็ก |
| `goethe.html` | **หน้าพิเศษ** ใบเซอร์ภาษาเยอรมัน (ไม่ใช้ `project.html`) |
| `js/projects-data.js` | **ไฟล์สำคัญที่สุด** ข้อมูลผลงานทุกชิ้นอยู่ที่นี่ที่เดียว |
| `js/portfolio.js` | วาดการ์ด + ปุ่มกรองในหน้า Hall of Frame |
| `js/project.js` | หน้ารายละเอียดผลงาน (สไลด์รูป, thumbnail, lightbox) |
| `js/goethe.js` | หน้าใบเซอร์ (กดขยายใบเซอร์) |
| `js/script.js` | ของที่ใช้ร่วมทุกหน้า (navbar, ม่านเปลี่ยนหน้า, สลับภาษา, reveal on scroll) |
| `css/style.css` | CSS ทั้งเว็บอยู่ไฟล์เดียว |
| `sitemap.xml` | แผนผังเว็บสำหรับ Google — **เพิ่มผลงานใหม่ต้องมาเพิ่ม `<url>` ที่นี่ด้วย** |
| `googlece4ecacc362ac4e9.html` | ไฟล์ยืนยันเจ้าของเว็บของ Google Search Console — **ห้ามลบ ห้ามแก้** ลบแล้วโดนถอนสิทธิ์ |
| `README.md` | คู่มือสำหรับ**เจ้าของเว็บ** วิธีแก้เนื้อหาทีละจุด |

---

## 3. ⚠️ กติกา 5 ข้อที่ห้ามพลาด

เรื่องพวกนี้เคยทำพังมาแล้วจริงๆ ในแชทก่อน แต่ละข้อมีที่มาจากบั๊กจริง

### 3.1 แก้ CSS หรือ JS → ต้องเปลี่ยนเลข `?v=` ทุกไฟล์ .html
GitHub Pages สั่งเบราว์เซอร์แคชไฟล์ 10 นาที (`max-age=600`) ถ้าไม่เปลี่ยนเลข
คนที่เคยเข้าเว็บจะได้ **HTML ใหม่ปนกับ CSS/JS เก่า** แล้วหน้าเว็บพัง

```bash
# ตอนนี้อยู่ที่ v=13 → ครั้งหน้าใช้ v=14 (ต้องเปลี่ยนให้ครบทั้ง 5 ไฟล์ .html)
for f in index.html portfolio.html about.html project.html goethe.html; do
  sed -i 's#css/style\.css?v=13#css/style.css?v=14#g' "$f"
  sed -i -E 's#(js/[a-z-]+\.js)\?v=13#\1?v=14#g' "$f"
done
```
*(แก้เฉพาะข้อความใน `.html` ไม่ต้องเปลี่ยนเลข)*

### 3.2 คลาสที่ใช้คู่กับ `.container` ห้ามเขียน `padding` แบบย่อ
`.container` ตั้ง `padding: 0 clamp(20px, 4vw, 48px)` ไว้เป็นระยะขอบซ้าย-ขวาของทั้งเว็บ
ถ้าเขียน `padding: 90px 0` ในคลาสที่ใช้คู่กัน เลข `0` จะไป**ล้างระยะขอบทิ้ง** เนื้อหาชิดขอบจอ
→ **ใช้ `padding-top` / `padding-bottom` แยกเสมอ** (`.section`, `.page-header`, `.footer` แก้แล้ว)

### 3.3 แตะ `localStorage` ต้องครอบ `try/catch` เสมอ
Safari โหมดส่วนตัว / เครื่องที่ปิดคุกกี้ จะ **throw ทันทีที่เรียกใช้**
ถ้าไม่ดัก error จะทะลุออกไปทำให้โค้ดถัดจากนั้น**ไม่ทำงานทั้งหมด** (เคยทำให้เนื้อหาทั้งหน้าหายมาแล้ว)
ตอนนี้มี 2 ที่ที่ใช้: `siteLang` (ภาษา) ใน `script.js` และ `hofFilter` (แท็บ) ใน `portfolio.js` — ทั้งคู่ครอบไว้แล้ว

### 3.4 อย่าให้ "การมองเห็นเนื้อหา" ขึ้นกับ JS หรืออนิเมชัน
ถ้า JS ไม่ทำงาน/โหลดช้า เนื้อหาต้องยังอ่านได้ ตัวอย่างที่ทำไว้แล้ว:
- **ระบบ 2 ภาษา** ซ่อน/แสดงด้วย CSS ผ่าน `data-active-lang` ที่แท็ก `<html>` (ไม่ใช่ให้ JS มา toggle class) ถ้า JS ตาย ก็ยังเห็นภาษาเดียว ไม่โผล่ซ้อนกัน 2 ภาษา
- **ม่านเปลี่ยนหน้า** จางหายเองด้วย CSS animation ถ้าให้ JS เปิดม่าน แล้ว JS ตาย = จอดำค้างทั้งหน้า
- **`prefers-reduced-motion`** ต้องเขียน `animation: none` + กำหนดสถานะปลายทางตรงๆ **ห้ามแค่ย่นเวลาให้สั้น** เพราะถ้าอนิเมชันไม่วิ่งจบ ตัวหนังสือจะค้างซ่อนอยู่ใต้กรอบแล้วมองไม่เห็นเลย

### 3.5 `<img>` ที่เขียน `src` ไว้ใน HTML ตรงๆ ต้องเช็ค `img.complete` ย้อนหลัง
เบราว์เซอร์เริ่มโหลดรูปตั้งแต่ก่อน JS ทำงาน ถ้ารูปพังไปก่อนแล้ว การมาติด `onerror` ทีหลังจะไม่ทำงาน
(เคยทำให้ 5 จาก 11 ใบเซอร์ไม่ขึ้นกล่องแจ้งเตือน) → ดูตัวอย่างที่แก้แล้วใน `js/goethe.js`
*(ส่วนรูปที่สร้างด้วย `document.createElement('img')` ไม่มีปัญหานี้ เพราะติด handler ก่อนตั้ง `src`)*

---

## 4. ระบบที่ทำไว้แล้ว (ทำงานได้ ทดสอบแล้ว)

- **ระบบผลงานรวมศูนย์** — แก้ `js/projects-data.js` ที่เดียว การ์ดกับหน้ารายละเอียดอัปเดตตามเอง
- **ปุ่มกรอง Mainframe / Other Skills** — ไม่มีแท็บ "All" (ตั้งใจ) และ**จำแท็บที่เลือกไว้** ตอนกดกลับมา
- **ป้ายหมวดหมู่หลายป้ายต่อผลงาน** — `category: ['Conference', 'International']`
- **ระบบ 2 ภาษา TH/EN** — ใช้ที่ `about.html` (คำโปรย/แนะนำตัว/เรื่องวัยเด็ก) และ `project.html` (รายละเอียดงาน + รางวัล) — **ชื่องานกับวันที่ตั้งใจไม่ให้สลับภาษา**
- **หน้ารายละเอียดผลงาน** — สไลด์รูป + แถบ thumbnail 5 รูป + กดขยายเต็มจอ + ปัดนิ้ว + ปุ่มลูกศร
- **แถบ "View Project" ท้ายการ์ด** — เห็นตลอดเวลา (เพราะมือถือไม่มี hover คนเลยไม่รู้ว่ากดได้)
- **เอฟเฟกต์ hover การ์ด** — รูปมืดลง + ขึ้นข้อความ เฉพาะเครื่องที่มีเมาส์จริง (`@media (hover: hover)`)
- **การ์ดพรีวิวตอนแชร์ลิงก์ (Open Graph)** — รูปอยู่ที่ `images/og-card.jpg` (1200×630)
- **อนิเมชัน** — ชื่อหน้าแรกเลื่อนขึ้นทีละบรรทัด + ม่านเปลี่ยนหน้า (CSS ล้วน)
- **หน้า `goethe.html`** — บันไดไล่ระดับ A1 → A2 → B1 (ผลงานที่ใส่ฟิลด์ `url` จะลิงก์ไปหน้าของตัวเอง)
- **ปุ่มลิงก์ในหน้ารายละเอียดผลงาน** — `paper:` → View Full Paper (PDF ในเว็บเรา) / `doi:` → View on IEEE Xplore (paper ตัวจริงบนเว็บสำนักพิมพ์)
  ปุ่มไหนไม่มีข้อมูลถูกลบทิ้งเอง ถ้าไม่เหลือปุ่มเลย กล่อง `.project-links` ก็ถูกลบด้วย
  **`--accent: #4f9fd4` เป็นสีเน้นสีเดียวของทั้งเว็บ** ที่เหลือขาวดำล้วน — อย่าเพิ่มสีอื่นโดยไม่ถาม
  *(เคยเป็นปุ่ม ORCID มาก่อน เปลี่ยนเพราะ ORCID เป็นแค่ป้ายชื่อที่เจ้าตัวกรอกเอง ส่วน DOI คือหลักฐานจากสำนักพิมพ์)*
- **SEO พื้นฐาน** — `sitemap.xml` (16 URL), `canonical` 4 หน้าหลัก, JSON-LD `Person` ใน `index.html`, ชื่อไทยใน title/footer/alt
  - **`project.html` ตั้งใจไม่ใส่ `canonical`** เพราะหน้านั้นแยกผลงานด้วย `?slug=` ถ้าใส่จะโดนยุบรวมเหลือหน้าเดียวใน Google
  - **ไม่มี `robots.txt` โดยตั้งใจ** — GitHub Pages แบบ project site อ่านจาก root ของ `panyakorn-pan.github.io` ซึ่งอยู่นอกรีโปนี้ ใส่ไปก็ไม่มีผล

---

## 5. งานที่ยังค้างอยู่

### รูปที่ยังขาด (เจ้าของเว็บต้องวางเอง ไม่ต้องแก้โค้ด)
- `images/certs/goethe/A2-1.jpg`, `A2-2.jpg`, `A2-3.jpg` — อีก 8 ใบวางครบแล้ว
  *(ใบที่ยังไม่มีรูปจะขึ้นกล่องลายทางบอกชื่อไฟล์ และกดขยายไม่ได้ — พฤติกรรมที่ตั้งใจ)*
- `images/projects/sports-1/1.jpg` — การ์ด placeholder ในแท็บ Other Skills

### เรื่องที่เจ้าของเว็บ "ตั้งใจให้เป็นแบบนี้" — อย่าไปแก้เอง
- **การ์ด 2 ใบใน Other Skills ใช้ชื่อซ้ำกันเป๊ะ** ("ตัวจริงเค้ายุ่งอยู่ ยังไม่ว่างมาลงงาน รอแปปนะคร้าบบบบ") — ตั้งใจทำกวนๆ ไว้รอลงงานจริง
- **งานอบรมขับรถแข่ง/ช่างแอร์ อยู่กลุ่ม `mainframe`** — เจ้าของจัดเอง

### รอเจ้าของเว็บทำเอง (โค้ดพร้อมแล้ว AI ทำแทนไม่ได้)
- **สมัคร Google Search Console + ส่ง sitemap + Request Indexing** — ณ 1 ส.ค. 2026 เว็บ**ยังไม่ถูก Google เก็บเข้าคลังเลย**
  (ค้น `"Panyakorn Singhadoung"` และ `"ปัณยกร สิงห์ดวง"` ไม่เจอเว็บนี้) ขั้นตอนละเอียดอยู่ใน `README.md` หัวข้อ SEO
  **ไม่มีคู่แข่งชื่อซ้ำเลย** — พอ Google เก็บได้จะขึ้นอันดับ 1 เอง ไม่ต้องทำอะไรเพิ่ม
- **เอาลิงก์เว็บไปวางที่ GitHub profile / bio โซเชียล** เพื่อให้ Google เจอเร็วขึ้น

### ที่คุยกันไว้แต่ยังไม่ได้ทำ (เสนอได้ถ้าเขาถาม)
- แถบตัวเลขสรุปผลงาน + อนิเมชันนับเลข ในหน้า Hall of Frame
- ฝังวิดีโอในหน้ารายละเอียดผลงาน (งานหุ่นยนต์ดูตอนขยับอิมแพคกว่ารูปนิ่ง)
- หน้า CV / Resume พร้อม print stylesheet (กด Ctrl+P ได้ PDF สวยๆ)

---

## 6. วิธีทำงานที่เจ้าของเว็บชอบ

- **คุยภาษาไทย** เขาไม่ใช่โปรแกรมเมอร์ อธิบายให้เข้าใจง่าย บอก "ทำไม" ไม่ใช่แค่ "ทำอะไร"
- **ลงมือทำเลย** ไม่ต้องถามเยอะ แต่ถ้าเป็นเรื่อง**เนื้อหา/ถ้อยคำของเขาเอง** ให้ถามก่อน
- **ทดสอบจริงเสมอ** เขาให้ความสำคัญกับการยืนยันว่าใช้งานได้จริง ไม่ใช่แค่บอกว่าแก้แล้ว
- **อัปเดต `README.md` ทุกครั้งที่เพิ่มระบบใหม่** เขาต้องแก้เว็บเองต่อได้โดยไม่ต้องพึ่ง AI
- **push ขึ้น GitHub ให้เลย** หลังแก้เสร็จ แล้วรอ deploy และตรวจบนเว็บจริง

```bash
git add -A && git commit -m "อธิบายสั้นๆ ว่าแก้อะไร" && git push
```

---

## 7. ข้อควรรู้เรื่องการทดสอบ (สำคัญ อ่านก่อนสรุปว่าเจอบั๊ก)

Browser preview pane ที่ใช้ทดสอบ **ไม่ render เฟรมจริง** ทำให้เกิดผลลวงตาแบบนี้:

| อาการที่เห็นตอนทดสอบ | ความจริง |
|---|---|
| `IntersectionObserver` ไม่ทำงาน → เนื้อหา opacity 0 หมดทั้งหน้า | ปกติ ไม่ใช่บั๊ก |
| CSS `transition` ไม่ขยับ ค่าค้างที่จุดเริ่มต้น | ปกติ — ถ้าจะวัดค่าปลายทาง ให้ inject `transition: none !important` ก่อน |
| CSS `animation` ค้างที่ `currentTime: 0` | ปกติ — ใช้ `getAnimations()` แล้วตั้ง `currentTime` เอง |
| `screenshot` ล้มเหลว "pane is not displayed" | ปกติ ใช้ `javascript_tool` วัดค่าตัวเลขแทน |
| ไฟล์ CSS/JS ที่แก้แล้วยังเป็นของเก่า | แคชของ pane — ให้ `fetch(..., {cache:'no-store'})` แล้ว inject เอง |

**วิธีแยกว่าเป็นบั๊กจริงหรือเครื่องมือ:** ทดสอบแบบเดียวกันบน localhost เทียบกับเว็บจริง
ถ้าได้ผลเหมือนกันทั้งคู่ = เครื่องมือ ไม่ใช่บั๊ก (เคยเกือบวินิจฉัยผิดมาแล้ว)

**เครื่องของเจ้าของเว็บปิดอนิเมชันของ Windows ไว้** (`MinAnimate = 0` ใน registry)
เบราว์เซอร์จึงรายงาน `prefers-reduced-motion: reduce` ถ้าเขาบอกว่า "ไม่เห็นอนิเมชันเลย"
ให้เช็คเรื่องนี้ก่อน — วิธีเปิดกลับเขียนไว้ใน `README.md` แล้ว
